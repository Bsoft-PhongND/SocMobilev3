import {theme} from '../theme/theme';

const pieChartModel = [
  {label: 'Pending', y: 35, color: theme.colors.hight},
  {label: 'Waiting', y: 40, color: theme.colors.medium},
  {label: 'Verified', y: 55, color: theme.colors.low},
];
export {pieChartModel};
